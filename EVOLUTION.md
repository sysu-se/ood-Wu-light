# Homework 2 设计演进文档

本文档记录 Homework 2 新增功能对原有设计的演进和影响。

## 1. 提示功能的实现

候选提示功能实现在 Sudoku 类的 getCandidates(row, col) 方法中。该方法遍历同行、同列、同宫的所有数字，收集已使用的数字集合，返回 1-9 中未被使用的数字列表。

下一步提示也实现在 Sudoku 类中。getNextHint() 方法遍历所有空格，找出候选数只有一个的单元格，返回其位置和推定值。getAllNextHints() 返回所有此类单元格。

加分项实现包括提示原因说明（每个提示带有 reason 字段）和区分位置与填值（getNextHint 返回位置和值但不直接填写，Game.applyHint 可选择直接填写）

## 2. 提示功能归属分析

我认为提示功能更属于 Sudoku。

原因分析：

提示是数独规则的体现。候选数的计算完全基于数独本身的规则（行、列、宫的约束），不涉及任何游戏状态，是数独作为逻辑游戏的固有属性。

数据依赖性方面，getCandidates 只依赖当前棋盘状态，不需要历史记录等会话信息。即使是静态的数独问题也能计算候选数。

复用性考量方面，提示功能可能在多个场景使用：求解器、验证器、教学工具。将其放在 Sudoku 层可独立复用，不必依赖 Game 会话。

单一职责原则方面，Sudoku 负责核心逻辑和数据，Game 负责会话管理和状态控制。提示属于核心逻辑，应归入 Sudoku。

Game 层提供便捷接口 getCandidatesHint 来委托调用 Sudoku 的方法，保持层次清晰。

## 3. 探索模式的实现

探索模式的本质是在主会话基础上创建独立的尝试分支，采用状态切换加分支会话的混合模式。

核心组件 ExploreBranch 包含：sudoku（探索分支的数独副本）、startPoint（探索起点在主历史中的索引）、history（探索过程的独立历史）、historyIndex（探索历史当前位置）、failed（是否已确认失败）。

主要流程：

进入探索 startExplore：保存当前状态的快照，创建新的 ExploreBranch，切换游戏状态为 EXPLORING。

探索中操作 guess：操作记录到分支的独立历史，检测冲突并标记失败状态。

提交探索 commitExplore：将有效操作合并到主历史，恢复 PLAYING 状态。

放弃探索 abortExplore：回退到探索起点，记录失败路径，清理分支状态。

## 4. 主局面与探索局面的关系

主局面和探索局面是独立的对象副本，采用深拷贝策略。

选择复制而非共享的原因：

隔离性：探索中的修改不应立即影响主局面，用户可放心尝试而不必担心破坏已有进度。

回滚简单性：放弃探索只需丢弃副本，无需复杂的撤销操作，主局面状态始终完整保留。

失败路径记忆：可独立记录每个失败分支的最终状态，不污染主局面的历史。

深拷贝通过 Sudoku.clone 实现，使用 map 和展开语法确保二维数组独立。

提交时将探索历史逐步重建到主历史，支持后续 Undo/Redo。放弃时直接恢复探索起点状态，瞬时回退。

## 5. History 结构的演进

Homework 1 采用线性栈结构，单一的历史记录数组配合索引指针标记当前位置。

Homework 2 的演进：主 History 保持线性栈结构，未改变基本设计。探索分支拥有独立的 history 数组和 historyIndex 指针。

历史的协作关系：主 History 记录主操作，探索分支记录探索操作。提交后探索操作追加到主 History 的末尾，形成完整的操作序列。

当前实现支持多个探索分支的创建和独立追踪、失败路径记忆。暂未实现 DAG 合并或多层嵌套探索。

## 6. Homework 1 设计暴露的局限

局限一：固定的不可修改语义。Homework 1 中 fixed 数组标记初始数字不可修改，在探索模式中限制了尝试任何候选值的灵活性。解决方案是保持不变，因为探索只是尝试，不能改变原始谜题的固定单元格定义。

局限二：线性 History 无法表达分支。Homework 1 的线性历史无法自然表达尝试 A 到失败到回溯到尝试 B 的过程。解决方案是引入 ExploreBranch 管理探索历史，主历史保持线性。

局限三：Undo/Redo 没有元数据。Homework 1 的历史只记录棋盘状态，探索模式需要操作描述等信息。解决方案是增加 description 字段，在探索历史中记录特定描述。

## 7. 如果重做 Homework 1 的设计修改

修改一：预先设计分支能力。如果预见探索模式的需求，会在 Homework 1 就引入 branches 数组和 activeBranch 字段预留分支结构。

修改二：历史记录包含更多元信息。HistoryEntry 增加 type 字段（main 或 explore 或 hint）和 branchId 字段，可更清晰地区分不同来源的操作。

修改三：状态机的显式建模。GameState 枚举增加 EXPLORING 状态，状态转换规则明确定义允许的转换路径。

修改四：分离视图状态和逻辑状态。棋盘逻辑状态 SudokuState 包含 grid 和 fixed，游戏会话状态 GameSession 包含 mainState、exploreState 和 history，可更灵活地在不同状态间切换。

保持不变的部分：Sudoku 作为核心领域对象的定位、Game 负责会话管理的职责划分、防御性拷贝和不可变性原则、线性历史的基本结构。

## 总结

Homework 2 的提示和探索功能推动了设计的演进：提示功能自然地归属于 Sudoku，强化了领域对象的职责边界；探索模式引入了分支会话概念，但保持主历史的线性结构；主局面与探索局面采用复制隔离策略，简化了合并和回滚逻辑；History 结构保持线性，通过 ExploreBranch 扩展分支能力。

整体设计保持了 Homework 1 的核心架构，通过增量扩展而非重写来满足新需求，体现了良好的设计演进能力。